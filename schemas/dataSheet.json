{
  "bsonType": "object",
  "required": ["text", "comfort", "versatility", "occasions", "heatTrainingLevel", "whoHasWornIt", "Shoes"],
  "properties": {
    "text": {
      "bsonType": "string",
      "description": "must be a string and is required"
    },
    "comfort": {
      "bsonType": "string",
      "description": "must be a string and is required"
    },
    "versatility": {
      "bsonType": "string",
      "description": "must be a string and is required"
    },
    "occasions": {
      "bsonType": "string",
      "description": "must be a string and is required"
    },
    "heatTrainingLevel": {
      "bsonType": "int",
      "description": "must be an integer and is required"
    },
    "whoHasWornIt": {
      "bsonType": "array",
      "items": {
        "bsonType": "string"
      },
      "description": "must be an array of strings and is required"
    },
    "Shoes": {
      "bsonType": "array",
      "items": {
        "bsonType": "objectId"
      },
      "description": "must be an array of objectIds and is required"
    }
  }
}
