{
  "$jsonSchema": {
    "bsonType": "object",
    "required": ["tag_id", "Shoes"],
    "properties": {
      "tag_id": {
        "bsonType": "string",
        "description": "must be a string and is required"
      },
      "Shoes": {
        "bsonType": "array",
        "items": {
          "bsonType": "objectId"
        },
        "description": "must be an array of objectIds and is required"
      }
    }
  }
}
