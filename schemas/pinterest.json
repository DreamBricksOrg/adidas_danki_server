{
  "bsonType": "object",
  "required": ["Shoe", "links"],
  "properties": {
    "Shoe": {
      "bsonType": "objectId",
      "description": "The ObjectId of the shoe associated with these Pinterest links"
    },
    "links": {
      "bsonType": "array",
      "items": {
        "bsonType": "string",
        "description": "Each link must be a string"
      },
      "description": "List of Pinterest links associated with the shoe"
    }
  }
}
